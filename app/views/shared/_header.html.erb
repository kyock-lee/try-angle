    <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand text-white" href="/">BODY MAINTENANCE</a>
          <button class="navbar-toggler bg-white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link text-white" href="#">体重の記録</a>
              </li>
              <li class="nav-item">
                <a class="nav-link text-white" href="#">食事の記録</a>
              </li>
              <li class="nav-item">
                <a class="nav-link disabled text-white" href="#" tabindex="-1" aria-disabled="true">トレーニングの記録</a>
              </li>
            </ul>
          </div>
          <ul class="navbar-nav text-nowrap text-white">
            <% if user_signed_in? %>
              <li class="p-3"><%= link_to current_user.nickname + %{さん}, "#" %></li>
              <li class="p-3"><%= link_to 'ログアウト', destroy_user_session_path, method: :delete %></li>
            <% else %>
              <li class="p-3"><%= link_to 'ログイン', new_user_session_path %></li> 
              <li class="p-3"><%= link_to '新規登録', new_user_registration_path %></li>
            <% end %>
          </ul>
        </div>
        
      </nav>
    </header>