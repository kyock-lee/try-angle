<%= render "shared/header" %>

<div>
  <h3 class="text-center m-3">
    <%= "#{current_user.nickname}さんの記録" %>
  </h3>

  <div>
    <div>
      <%= Training.where(id: 1) %>
    </div>
  </div>

</div>

<%= render "shared/footer" %>